{
  "name": "mcp-server",
  "version": "1.0.0",
  "type": "module",
  "main": "build/src/index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start:dev": "npx nodemon --exec \"npm run build && node build/src/index.js\" --ext ts --watch src",
    "start": "npm run build && node build/src/index.js",
    "lint": "eslint . --ext .ts"
  },
  "author": "Token Metrics",
  "license": "ISC",
  "description": "MCP Server for Token Metrics API",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.7.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^18.6.1",
    "esbuild": "^0.25.4",
    "eslint": "^8.20.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-jest": "^26.6.0",
    "eslint-plugin-prettier": "^4.2.1",
    "nodemon": "^3.0.0",
    "prettier": "^2.7.1",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.0"
  }
}
